import styled from 'styled-components/native';
import styles from '../../constants/styles';

export const Container = styled.View`
  flex: 1;
  background-color: ${styles.screensBackgroundColor};
  justify-content: center;
  align-items: center;
`;
